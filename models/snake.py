class Snake():
    """Creates a Snake instance that meets the API"""
    def __init__(self, id, name, owner_id, species_id, gender, color):
        self.id = id
        self.name = name
        self.owner_id = owner_id
        self.species = species_id
        self.gender = gender
        self.color = color
